
## From plans

### orchestration-foundations
- `--json` flag on all commands for machine-consumable output
- `pickNext()` for priority-based plan selection
- Status filtering (`--all`, `--done`, `--archived`)
- Epic tracking via tag-based grouping
