
## From plans

### plan-schema
- `PlanFile` enum, `SECTION_REQUIREMENTS`, `STATUS_GATES` — canonical plan structure
- `StatusGate` interface defining required files and sections per status transition
