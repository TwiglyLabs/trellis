
## From plans

### plan-schema
- `PlanFile` enum and `SECTION_REQUIREMENTS` — required headings per file
- `STATUS_GATES` — required files and sections per status transition
- `validateStatusGate()` — gate checking logic
