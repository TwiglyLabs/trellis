# Inputs

## From existing code

### src/commands/graph.ts
- `graphCommand` function (lines 38-115) — the server startup and browser launch logic
- `execFile` call on lines 111-113 that opens the browser

### src/cli.ts
- Commander program definition for the `graph` subcommand (lines 71-77)
