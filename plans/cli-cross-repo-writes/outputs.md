
## Exports
- `resolveCliContext(projectDir)` function exported from `src/core/cli-context.ts` — returns multi-repo or single-repo context based on manifest availability
- CLI `create`, `set`, `update` commands accept qualified IDs for cross-repo operations
- `createCliFixtureWithManifest()` test helper for multi-repo CLI testing
