
## From plans

### feature-slices-batch-1
- `src/features/` vertical slice pattern established for status, ready, show, update, lint, create
- Feature directory convention: `logic.ts` (pure compute) + `command.ts` (CLI handler) + `*.test.ts`
