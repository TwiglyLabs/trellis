
## From plans

### context-store-core
- `ContextStore` class — `load()`, `get()`, `watch()`, `invalidate()`, `persist()`
- `watch()` with `WatchHandle` for live fs.watch integration in long-running MCP server
