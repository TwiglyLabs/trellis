
## From plans

### context-store-core
- `ContextStore` class — `load()`, `get()`, `persist()`, `invalidate()`
- `computeMtimeHash()` for cache validation
- `PlanIndex` / `RepoIndexEntry` types for the index schema
